import { createMuiTheme } from '@material-ui/core/styles';
export const COLOUR_THEME = createMuiTheme({
  palette: {
    primary: {
      main: '#7F003F'
    },
    secondary: {
      main: '#b3b3b3'
    },
  },
});